package config;

import java.lang.Math; // headers MUST be above the first class // one class needs to have a main()
						// method public class HelloWorld { public static void main(String[] args) { int
						// tab[] = new int[] {1,2,3}; int tab2[] = new int[] {1,1,1}; int wellPlaced =
						// 0; int badPlaced = 0; for (int i = 0; i < tab.length; i++){ if (tab[i] ==
						// tab2[i]){ tab[i] = -1; wellPlaced++; } } for (int i = 0; i < tab.length;
						// i++){ if (tab[i] != -1){ for (int j = 0; j < tab.length; j++){ if (tab[i] ==
						// tab2[j]){ tab2[j] = -1; badPlaced++; } } } } System.out.println(wellPlaced +
						// " " + badPlaced); } }
